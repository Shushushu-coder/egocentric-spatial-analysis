numpy>=1.24,<2
# Video I/O: install exactly one OpenCV wheel (this, or opencv-python-headless).
# Do not combine with opencv-contrib-python; a leftover empty cv2/ namespace
# makes import succeed without VideoCapture.
opencv-python>=4.8
scipy>=1.11
matplotlib>=3.8
